# Minimal dependencies for Railway (CPU-only, lightweight)
# PyTorch CPU-only minimal version
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchaudio==2.1.0+cpu

# NumPy < 2.0 (compatibilidade com PyTorch)
numpy<2.0

# Audio processing (lightweight)
soundfile>=0.12.0
librosa>=0.10.0

# Web UI - Gradio 4.x (estável, sem bugs do 6.x)
gradio>=4.0.0,<5.0.0

# HuggingFace Hub - versão compatível com Gradio 4.x
# HfFolder foi removido em versões >= 0.24.0
huggingface_hub>=0.19.0,<0.24.0

# Utilities
tqdm>=4.64.0
